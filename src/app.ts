import { request } from './plugins/request/request';

import rootContainer from './root-container';

// 相关配置已经预设，但是需要提供后端接口，所以先注释掉
// export { qiankun } from './qiankun/entry';

export { request, rootContainer };
